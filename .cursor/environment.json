{
  "name": "SideTerm",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "user": "ubuntu",
  "install": "git submodule update --init --recursive && cargo build -p wezterm-gui -p wezterm -p wezterm-mux-server -p strip-ansi-escapes"
}
